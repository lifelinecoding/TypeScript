// Type Annotations :- Type annotaion means we have to define the type while declaration
let name: string = "Aditya Patel";
const rand: number = Math.random() > 0.5 ? 10 : 5;

// Type Inference :- Type inference means it automatically understands which type of data will be assigned
let user = "Aditya Patel";
